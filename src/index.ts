/**
 * @amlplugins/hubspot-crm-deals
 *
 * Thin re-export of @hubspot/api-client (CommonJS `export =` SDK).
 *
 * HubSpot CRM Deals — pipelines, stages, deal properties, associations, search.
 */

import _sdk from "@hubspot/api-client";
export { _sdk as sdk };
export default _sdk;
